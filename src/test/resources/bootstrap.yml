spring:
  application:
    name: zuul-issue-repro
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
    bus:
      enabled: false
    stream:
      kafka.binder:
        brokers: localhost:15555
        zkNodes: localhost:15556

eureka:
  client:
    enabled: false

ribbon:
  eureka:
    enabled: false
